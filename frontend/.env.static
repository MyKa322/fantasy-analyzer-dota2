# Режим опубликованной страницы: данные берутся из снапшота, бэкенд не нужен.
# Локально запускается как `npm run dev:static` — видно ровно то же, что на
# GitHub Pages, включая пересчёт эмблем в браузере.
VITE_STATIC_DATA=1
